source-repo: ElementalEngine/core-api-backend
source-sha: 9739b2b
source-date: 2026-09-13

generated-by: npm run gen:contracts

The join between this spec and core-api's live app is NOT machine-checked
(D98). It is bumped as a deliberate, reviewable act. Two diffs chain around
it: core-api CI asserts app.openapi() == its own openapi.json; this repo's
CI asserts gen:contracts --check produces no diff against src/.

Bumped at Phase 3 S10: 0898efd -> b4f7a67. D220 moved every bot-reachable
lobby route under /api/v2/lobbies/mite and added browse, cancel, leave and
sweep there; the bans phase carries its caps and the settings phase its
questions.
